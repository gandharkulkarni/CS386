// Float variables
let op1 = 23.56;
let op2 = 83.56;

//Addition
let sum = op1 + op2;

//Subtraction
let diff = op2 - op1;

//Division
let quotient = op1/op2;

//Multiplication
let product = op1*op2;

//Interpolated string output
console.log(`The sum of ${op1} and ${op2} = ${sum}
The difference of ${op2} and ${op1}  = ${diff}
The quotient of ${op1} and ${op2} = ${quotient}
The product of ${op1} and ${op2} = ${product}`);